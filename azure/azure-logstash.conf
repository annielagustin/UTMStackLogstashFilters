filter {
    if [logx][type] == "azure" {
        mutate {
            add_field => {
                "dataType" => "azure"
            }
            rename => {
                "host" => "dataSource"
            }
        }

        mutate {
            remove_field => ["headers", "[logx][type]", "@version", "global", "es_metadata_id"]
        }

    }
}